# Database connection (shared with scraper)
DATABASE_URL="postgresql://USER:PASSWORD@localhost:5432/wp_connect?schema=public"

# Node environment
NODE_ENV="production"

# Optional: Port for Next.js (default 3000)
PORT=3000
